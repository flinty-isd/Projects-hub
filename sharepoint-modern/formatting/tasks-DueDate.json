{
  "$schema": "https://developer.microsoft.com/json-schemas/sp/v2/column-formatting.schema.json",
  "elmType": "div",
  "txtContent": "=if(@currentField == '', '', toLocaleDateString(@currentField))",
  "attributes": {
    "title": "=if(@currentField != '' && Number(@currentField) < Number(@now) && [$Status] != 'Done', 'Past due', '')"
  },
  "style": {
    "color": "=if(@currentField != '' && Number(@currentField) < Number(@now) && [$Status] != 'Done', '#a4262c', '')",
    "font-weight": "=if(@currentField != '' && Number(@currentField) < Number(@now) && [$Status] != 'Done', '600', 'normal')"
  }
}
